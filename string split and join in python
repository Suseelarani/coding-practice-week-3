def split_and_join(line):
    # Split the string into a list using space as the delimiter
    words = line.split(" ")
    # Join the list into a string using hyphen as the delimiter
    return "-".join(words)

if __name__ == '__main__':
    line = input()
    result = split_and_join(line)
    print(result)
